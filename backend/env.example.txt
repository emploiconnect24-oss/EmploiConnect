# ============================================================
# CONFIGURATION SUPABASE - EMPLOICONNECT
# Copiez ce contenu dans un fichier .env dans le dossier backend/
# ============================================================

# Supabase Project URL
SUPABASE_URL=https://xxxxx.supabase.co

# Supabase API Keys
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Database Connection (PostgreSQL)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRES_IN=7d

# Storage Configuration
SUPABASE_STORAGE_BUCKET=cv-files

# Application Configuration
PORT=3000
NODE_ENV=development

# CORS : par défaut le backend autorise tout http://localhost:* (Flutter Web = port aléatoire).
# En production, mettez CORS_STRICT=1 et listez uniquement vos domaines dans CORS_ORIGIN.
CORS_ORIGIN=http://localhost:3000,http://localhost:8080
# CORS_STRICT=1

# ============================================================
# NLP via RapidAPI (optionnel mais recommandé pour un projet pertinent)
# Sans clé : extraction locale (mots-clés) + score par recoupement de termes.
# Avec clé : parsing de CV (compétences, expérience) + similarité sémantique pour le matching.
# Obtenir une clé gratuite : https://rapidapi.com/ → s'inscrire → souscrire aux APIs (plans gratuits)
# ============================================================
# RAPIDAPI_KEY=votre_cle_rapidapi
# Text Similarity (ex: Twinword) - pour le score candidat/offre
# RAPIDAPI_SIMILARITY_HOST=twinword-text-similarity.p.rapidapi.com
# Resume Parser - pour extraire compétences/expérience du CV (PDF/DOCX)
# RAPIDAPI_RESUME_PARSER_HOST=resumeparser-api.p.rapidapi.com
# RAPIDAPI_RESUME_PARSER_PATH=
# ============================================================

# ============================================================
# INSTRUCTIONS
# ============================================================
# 1. Créez un fichier .env dans le dossier backend/
# 2. Copiez ce contenu dans le fichier .env
# 3. Remplissez les valeurs avec vos clés Supabase
# 4. Ne commitez JAMAIS le fichier .env dans Git !
# ============================================================

