# ============================================================
# CONFIGURATION SUPABASE - EMPLOICONNECT
# Copiez ce contenu dans un fichier .env dans le dossier backend/
# ============================================================

# Supabase Project URL
SUPABASE_URL=https://xxxxx.supabase.co

# Supabase API Keys
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Database Connection (PostgreSQL)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRES_IN=7d

# Chiffrement des clés API stockées dans parametres_plateforme (min. 16 caractères)
ENCRYPTION_KEY=change_me_minimum_16_chars_secret

# Buckets Storage (paramètres admin : bannières homepage + logos)
SUPABASE_BANNIERES_BUCKET=bannieres
SUPABASE_LOGOS_BUCKET=logos

# Storage Configuration
SUPABASE_STORAGE_BUCKET=cv-files
# Bucket pour photos de profil (images). Requis si cv-files n’accepte que les PDF.
# Créer dans Supabase → Storage → New bucket (ex. avatars), « Allowed MIME types » vide = tout autoriser.
# SUPABASE_STORAGE_BUCKET_AVATARS=avatars
# Bucket optionnel dédié aux pièces jointes messagerie (recommandé, public).
# S'il n'est pas défini, le backend bascule automatiquement entre logos / cv-files / avatars selon MIME.
# SUPABASE_STORAGE_BUCKET_MESSAGES=messagerie-files
# Bucket optionnel pour documents (pdf/docx/txt) en messagerie.
# SUPABASE_STORAGE_BUCKET_DOCS=cv-files

# Application Configuration
PORT=3000
NODE_ENV=development

# Limite de requêtes / fenêtre (IP) — augmenter si 429 trop fréquent (messagerie, polling).
# RATE_LIMIT_MAX=2000

# Citations tableau de bord candidat : configurées dans l’admin (Général), y compris API ZenQuotes / Quotable / URL custom.
# Aucune variable d’environnement requise.

# URL du front (liens emails : reset MDP, alertes). Surcharge partielle de parametres_plateforme.url_application_publique.
# PUBLIC_APP_URL=https://votre-app.gn

# Push FCM (API legacy HTTP v1 server key — optionnel)
# FCM_SERVER_KEY=

# Désactiver les tâches planifiées (ex. résumé hebdo lundi)
# DISABLE_CRON=1

# CORS : par défaut le backend autorise tout http://localhost:* (Flutter Web = port aléatoire).
# En production, mettez CORS_STRICT=1 et listez uniquement vos domaines dans CORS_ORIGIN.
CORS_ORIGIN=http://localhost:3000,http://localhost:8080
# CORS_STRICT=1

# Emails transactionnels (fallback .env + admin SMTP)
EMAIL_FROM=votre_vraie_adresse@gmail.com
EMAIL_USER=votre_vraie_adresse@gmail.com
EMAIL_PASSWORD=votre_mot_de_passe_application
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
EMAIL_NOM_PLATEFORME=EmploiConnect
# Interrupteurs notif_email_* : admin + migrations SQL (parametres_plateforme).

# ============================================================
# NLP via RapidAPI (optionnel mais recommandé pour un projet pertinent)
# Sans clé : extraction locale (mots-clés) + score par recoupement de termes.
# Avec clé : parsing de CV (compétences, expérience) + similarité sémantique pour le matching.
# Obtenir une clé gratuite : https://rapidapi.com/ → s'inscrire → souscrire aux APIs (plans gratuits)
# ============================================================
# RAPIDAPI_KEY=votre_cle_rapidapi
# Text Similarity (Twinword) - score candidat/offre
# RAPIDAPI_SIMILARITY_HOST=twinword-text-similarity-v1.p.rapidapi.com
# Resume Parser - extraction compétences/expérience CV
# RAPIDAPI_RESUME_PARSER_HOST=resume-parser3.p.rapidapi.com
# Topic Tagging - extraction mots-clés d'offres
# RAPIDAPI_TOPIC_TAGGING_HOST=twinword-topic-tagging1.p.rapidapi.com
# Seuil minimum pour suggestions IA (%)
# IA_SEUIL_MATCHING=40
#
# Alertes e-mail « nouvelle offre » (candidats) : en plus des alertes sauvegardées,
# similarité texte profil / offre (RapidAPI si clé, sinon recoupement de mots).
# ALERTE_IA_SIMILARITY_MIN=0.34
# ALERTE_FALLBACK_JACCARD_MIN=0.075
# ALERTE_IA_MAX_CANDIDATS_PAR_OFFRE=70
# ALERTE_PROFIL_IA_ENABLED=0   # mettre 0 pour désactiver le matching profil (garder seulement alertes)
# ============================================================

# ============================================================
# INSTRUCTIONS
# ============================================================
# 1. Créez un fichier .env dans le dossier backend/
# 2. Copiez ce contenu dans le fichier .env
# 3. Remplissez les valeurs avec vos clés Supabase
# 4. Ne commitez JAMAIS le fichier .env dans Git !
# ============================================================

